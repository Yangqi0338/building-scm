<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <artifactId>building-scm</artifactId>
    <version>${scm.revision}</version>
    <packaging>jar</packaging>
    <name>building-scm</name>
    <description>building-scm - SCM 后端入口 starter (聚合 Base biz-action + 装配身份 plugin)</description>

    <parent>
        <groupId>com.newzkl.platform</groupId>
        <artifactId>Base</artifactId>
        <version>1.0.1-SNAPSHOT</version>
        <relativePath/>
    </parent>

    <properties>
        <scm.revision>1.0.0-SNAPSHOT</scm.revision>
        <base.revision>1.0.1-SNAPSHOT</base.revision>
        <plugin.revision>1.0.0-SNAPSHOT</plugin.revision>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>com.newzkl.platform</groupId>
                <artifactId>Base</artifactId>
                <version>${base.revision}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>com.newzkl.platform</groupId>
                <artifactId>Plugin</artifactId>
                <version>${plugin.revision}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring-boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>com.newzkl.platform.Base.core</groupId>
            <artifactId>core-job-infrastructure</artifactId>
        </dependency>

        <!-- 身份 plugin 装配 (部署级身份开关: 装哪几个 = 支持哪些身份) -->
        <dependency>
            <groupId>com.newzkl.platform</groupId>
            <artifactId>plugin-channel</artifactId>
        </dependency>
        <dependency>
            <groupId>com.newzkl.platform</groupId>
            <artifactId>plugin-supplier</artifactId>
        </dependency>
        <dependency>
            <groupId>com.newzkl.platform</groupId>
            <artifactId>plugin-operator</artifactId>
        </dependency>
        <dependency>
            <groupId>com.newzkl.platform</groupId>
            <artifactId>plugin-hdh</artifactId>
        </dependency>
        <dependency>
            <groupId>com.newzkl.platform</groupId>
            <artifactId>plugin-openapi</artifactId>
        </dependency>
        <dependency>
            <groupId>com.newzkl.platform</groupId>
            <artifactId>plugin-audit</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-starter-bootstrap</artifactId>
        </dependency>

        <dependency>
            <groupId>com.alibaba.cloud</groupId>
            <artifactId>spring-cloud-starter-alibaba-nacos-config</artifactId>
        </dependency>
        <dependency>
            <groupId>com.alibaba.cloud</groupId>
            <artifactId>spring-cloud-starter-alibaba-nacos-discovery</artifactId>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <version>${spring-boot.version}</version>
                <configuration>
                    <mainClass>com.newzkl.platform.app.scm.ScmApplication</mainClass>
                </configuration>
                <executions>
                    <execution>
                        <goals>
                            <goal>repackage</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
