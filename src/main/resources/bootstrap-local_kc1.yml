spring:
  cloud:
    nacos:
      discovery:
        ip: 192.168.30.85
        port: 8087
        namespace: scm-dev

dubbo:
  registry:
    parameters:
      namespace: scm-dev
  provider:
    parameters:
      namespace: scm-dev
  consumer:
    parameters:
      namespace: scm-dev

auto-table:
  enable: true

server:
  port: 9002