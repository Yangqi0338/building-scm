spring:
  cloud:
    config:
      # 设为 true 后，远程配置不会覆盖任何已有的本地配置
      override-none: true
    nacos:
      discovery:
        ip: 124.70.145.191
        port: 8087
        namespace: scm-dev

dubbo:
  registry:
    parameters:
      namespace: scm-dev
  provider:
    parameters:
      namespace: scm-dev
  consumer:
    parameters:
      namespace: scm-dev

auto-table:
  enable: true

